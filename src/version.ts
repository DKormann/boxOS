// BOXOS follows semantic versioning while it is pre-1.0. Minor releases may
// intentionally change application APIs; the immutable runtime version changes
// only when stored source semantics change.
export const BOXOS_VERSION = "0.2.0";
export const BOXOS_RUNTIME_VERSION = 1;
